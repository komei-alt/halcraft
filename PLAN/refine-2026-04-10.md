# Sculpt: ハルクラ全体構造分析

```
Generated: 2026-04-10
Scope: halcraft/src/ (プロジェクト全体)
Total Files: 62
Total Lines: 17,152
```

---

## 構造マップ

### レイヤー構成

| レイヤー | ファイル数 | 総行数 | 備考 |
|---------|-----------|--------|------|
| 3D Components | 16 | 5,649 | Player, World, MobManager 等 |
| UI Components | 18 | 4,006 | StartScreen, CraftingScreen 等 |
| Mobile UI | 6 | 588 | Joystick, JumpButton 等 |
| Vehicles | 3 | 1,613 | Helicopter, MachineGun, CockpitView |
| Mobs | 5 | 751 | Zombie, Spider, Chicken 等（個別描画） |
| Stores | 8 | 1,881 | Zustand ストア |
| Utils | 8 | 1,908 | terrain, sounds, voiceChat 等 |
| Types | 3 | 504 | blocks, crafting, skins |
| Data | 1 | 103 | updateLog |
| Entry | 2 | 195 | main.tsx, App.tsx |

### ホットスポット（肥大化ファイル TOP 10）

| # | ファイル | 行数 | import数 | store参照数 | 問題 |
|---|---------|------|---------|------------|------|
| 1 | `MobManager.tsx` | 882 | 17 | 29 | **5種のモブAI + 物理 + スポーン + 死亡処理が1ファイルに混在** |
| 2 | `MachineGun.tsx` | 872 | 10 | 16 | 射撃物理 + 弾道描画 + 衝突エフェクト + 銃モデルが混在 |
| 3 | `terrain.ts` | 721 | 6 | — | 地形生成 + 家 + ヘリポート + 村 + 木配置が1ファイル |
| 4 | `Player.tsx` | 675 | 9 | 23 | 入力 + 物理 + カメラ + 搭乗が混在 |
| 5 | `CraftingScreen.tsx` | 624 | 5 | 10 | クラフトUI + ロジック + レシピ表示が混在 |
| 6 | `voiceChat.ts` | 560 | 3 | — | WebRTC P2P + 発話検出 + シグナリングが混在 |
| 7 | `useMultiplayerStore.ts` | 543 | 10 | 18 | **5ストアを直接 import、全ソケットイベントを1ファイルで処理** |
| 8 | `TurretRenderer.tsx` | 529 | 9 | 8 | タレット描画 + 弾道物理 + エフェクトが混在 |
| 9 | `Helicopter.tsx` | 497 | 8 | 7 | ヘリ3Dモデル + 操縦物理 + 乗降ロジックが混在 |
| 10 | `CockpitHUD.tsx` | 484 | — | 5 | コクピットUI（単一責務だが行数が多い） |

### 依存集中点

| ファイル | 被参照数 | 役割 |
|---------|---------|------|
| `types/blocks.ts` | 21 | ブロック定義（正当な共通型） |
| `stores/useVehicleStore.ts` | 13 | 乗り物状態（export 11個 = 責務過多） |
| `stores/useMobStore.ts` | 14 | モブ状態 |
| `utils/device.ts` | 10 | デバイス判定 |
| `stores/useMultiplayerStore.ts` | 12 | マルチプレイ（全ストアをimport） |


---

## 診断レポート

### 統計サマリー

| 指標 | 値 |
|------|-----|
| 総ファイル数 | 62 |
| 肥大化ファイル（>300行） | 17 |
| 超肥大化ファイル（>500行） | 7 |
| 過剰export（>5 exports） | 6 |
| ストア間相互依存 | 6箇所 |

### 病理一覧（重要度順）

---

#### [P1] 混在: `MobManager.tsx` — 882行

> [!CAUTION]
> プロジェクト最大のファイル。5種のモブのAI戦略 + 物理エンジン + スポーン管理 + 死亡イベント処理が巨大な1コンポーネントに詰め込まれている。

**混在する責務:**

| 責務 | 行数（推定） | 内容 |
|------|------------|------|
| 定数定義 | ~70 | 5種のモブ別パラメータ |
| AABB衝突判定 | ~50 | `checkMobCollision` / `checkMobCollisionSize` |
| ニワトリAI | ~90 | パッシブ行動 + ワンダリング |
| クモAI | ~90 | 攻撃的追跡 + 段差対応 |
| ゾンビAI | ~120 | 追跡 + 分離 + 回り込み |
| 味方AI（プロト/ゴーレム） | ~200 | 追従 + スタック検出 + 怒り状態 + 敵討伐 |
| ゴーレムスポーン | ~30 | SPAWNERブロック検索 |
| 死亡処理 | ~30 | エフェクト + サウンド + ドロップ |
| 描画ディスパッチ | ~20 | JSX switch文 |

**推奨: Split（分離）**
- `utils/mobPhysics.ts` — AABB衝突チェック（Player.tsx と共通化可能）
- `utils/mobAI/` — モブ種別ごとのAI関数
  - `zombieAI.ts`, `spiderAI.ts`, `chickenAI.ts`, `allyAI.ts`
- `MobManager.tsx` — スポーン管理 + AI呼び出し + 描画ディスパッチのみ（~200行目標）

---

#### [P2] 混在: `terrain.ts` — 721行

> [!WARNING]
> 地形生成、ノイズ関数、構造物6種（家・ヘリポート・村・木・道）のジェネレーターが全て1ファイル。

**混在する責務:**

| 責務 | 行数（推定） | 内容 |
|------|------------|------|
| ノイズ + FBM | ~30 | コア地形アルゴリズム |
| 高さ計算 + キャッシュ | ~25 | `getTerrainHeight` |
| 木配置 | ~60 | `shouldPlaceTree`, `placeTreesInChunk` |
| プレイヤーの家 | ~140 | `placePlayerHouse` |
| ヘリポート | ~70 | `placeHeliport` |
| 村 + 道 | ~200 | `placeVillage*` 3関数 |
| チャンク生成 | ~55 | `generateChunk` |
| 露出面判定 | ~50 | `isBlockExposed` |

**推奨: Split（分離）**
- `utils/terrain/noise.ts` — FBM + ノイズ関数
- `utils/terrain/heightmap.ts` — 高さ計算 + キャッシュ
- `utils/terrain/structures/` — 構造物ごとのファイル
  - `house.ts`, `heliport.ts`, `village.ts`, `trees.ts`
- `utils/terrain/index.ts` — generateChunk + isBlockExposed（エクスポート集約）

---

#### [P3] 混在: `useMultiplayerStore.ts` — 543行

> [!WARNING]
> 5つの他ストアを直接 import し、全ソケットイベントのハンドラが1つの `setupSocketListeners` 関数（250行）に集約されている。

**混在する責務:**

| 責務 | 行数（推定） | 内容 |
|------|------------|------|
| ストア定義 | ~150 | 状態 + アクション |
| プレイヤー同期 | ~100 | join/leave/move |
| ブロック同期 | ~30 | block:changed/world:changes |
| モブ同期 | ~20 | mob:sync |
| ヘリ同期 | ~60 | helicopter:sync |
| 機関銃同期 | ~20 | gun:fire |
| PvP | ~20 | player:attack/died/respawned |
| バージョン + ボイス | ~30 | server:version/voice:mic-status |
| コールバック管理 | ~20 | `onRemoteGunFire` |

**推奨: Split（分離）**
- `stores/multiplayer/useMultiplayerStore.ts` — コアの接続状態 + プレイヤー管理のみ
- `stores/multiplayer/socketHandlers.ts` — イベントハンドラ（ドメインごとにセクション分け）
- `stores/multiplayer/remoteCallbacks.ts` — コールバック管理

---

#### [P4] 重複: 衝突判定ロジック

> [!IMPORTANT]
> `MobManager.tsx` と `Player.tsx` にほぼ同一のAABB衝突判定が重複している。

- `MobManager.tsx:109-133` — `checkMobCollision`（固定サイズ）
- `MobManager.tsx:136-160` — `checkMobCollisionSize`（可変サイズ）
- `Player.tsx` 内にも同様の衝突判定

**推奨: Extract（抽出）**
- `utils/collision.ts` — 共通AABB衝突判定ユーティリティに統合

---

#### [P5] 重複: 弾道物理エンジン

> [!IMPORTANT]
> `MachineGun.tsx`（872行）と `TurretRenderer.tsx`（529行）が**ほぼ同一の弾道物理システム**を持つ。

共通パターン:
- Projectile 型定義
- ImpactEffect 型定義
- レイマーチングでブロック衝突判定
- モブとの球体交差判定
- トレイル描画コンポーネント
- インパクトパーティクルコンポーネント

**推奨: Extract（抽出）**
- `utils/projectilePhysics.ts` — Projectile/Impact型 + 衝突判定ロジック
- `components/effects/ProjectileTrail.tsx` — 共通トレイル描画
- `components/effects/ImpactParticles.tsx` — 共通インパクトエフェクト
- `MachineGun.tsx` / `TurretRenderer.tsx` — 武器固有のパラメータと銃モデルのみ

---

#### [P6] 混在: `MachineGun.tsx` — 872行

4つの内部コンポーネント（`DoorMountedGun`, `GunPivot`, `ProjectileTrail`, `ImpactParticles`）が同一ファイルに混在。P5で弾道部分を抽出後、銃モデル部分も分離推奨。

**推奨: Split + Extract（P5と連動）**

---

#### [P7] 抽象不足: エフェクト系コンポーネント

- `BlockBreakEffect.tsx`（204行）
- `MobDeathEffect.tsx`（177行）
- `DamagePopup.tsx`（160行）

パーティクルの生成・アニメーション・消滅ライフサイクルが各ファイルで独自実装。共通の「パーティクルエフェクトマネージャー」パターンが存在しない。

**推奨: 現状維持（低優先度）**
各エフェクトの見た目が異なるため、無理な共通化はしない。P5の弾道エフェクト共通化が先。

---

#### [P8] ストア間相互依存

`useMultiplayerStore` が5つの他ストアを import:
```
useMultiplayerStore → useWorldStore
useMultiplayerStore → useGameStore
useMultiplayerStore → useMobStore
useMultiplayerStore → usePlayerStore
useMultiplayerStore → useVehicleStore
```

`useGameStore` が `usePlayerStore` を import:
```
useGameStore → usePlayerStore
```

**推奨: P3の分離で改善**
ソケットハンドラをストアの外に出すことで `import` を分散させる。

---

## 再構築プラン

### Phase 1: 低リスク抽出（即実行可能）

影響範囲が小さく、テスト不要な構造変更。

- [ ] **P4: 衝突判定の共通化**
  - `utils/collision.ts` を新規作成
  - `MobManager.tsx` と `Player.tsx` から衝突判定関数を移動
  - 既存の呼び出し元を import 変更のみ
  - 影響: 2ファイルの import 変更

- [ ] **P5: 弾道物理の共通化**
  - `utils/projectilePhysics.ts` を新規作成（Projectile + Impact 型 + 衝突判定）
  - `components/effects/ProjectileTrail.tsx` を新規作成
  - `components/effects/ImpactParticles.tsx` を新規作成
  - `MachineGun.tsx` と `TurretRenderer.tsx` を共通モジュールに依存させる
  - 影響: 2ファイルの大幅削減（各 -300行程度）

### Phase 2: 中リスク分離（動作確認必要）

ゲーム動作に影響する可能性がある構造変更。

- [ ] **P2: terrain.ts の分割**
  - `utils/terrain/` ディレクトリに分割
  - `index.ts` で既存の export を維持（破壊的変更なし）
  - 影響: import パスの変更は `index.ts` 再エクスポートで回避可能

- [ ] **P1: MobManager.tsx の分割**
  - `utils/mobAI/` ディレクトリに各モブのAI関数を抽出
  - `MobManager.tsx` はスポーン管理と描画ディスパッチのみに
  - 影響: ゲームプレイに直結するため慎重にテスト

- [ ] **P3: useMultiplayerStore.ts の分割**
  - ソケットハンドラを別ファイルに抽出
  - ストア本体はスリム化
  - 影響: マルチプレイ動作テスト必要

### Phase 3: 高リスク再設計（承認必要）

なし（現時点では根本的な設計変更は不要）

## 期待される改善

| 指標 | Before | After（推定） | 改善率 |
|------|--------|-------------|--------|
| 300行超ファイル数 | 17 | 8 | -53% |
| 500行超ファイル数 | 7 | 2 | -71% |
| MobManager.tsx | 882行 | ~200行 | -77% |
| MachineGun.tsx | 872行 | ~350行 | -60% |
| terrain.ts | 721行 | ~100行（index） | -86% |
| useMultiplayerStore.ts | 543行 | ~250行 | -54% |
| コード重複（衝突判定） | 2箇所 | 1箇所 | -50% |
| コード重複（弾道物理） | 2箇所 | 1箇所 | -50% |
